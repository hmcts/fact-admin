{% from "govuk/components/details/macro.njk" import govukDetails %}

<h2 class="govuk-heading-l">Postcodes</h2>

In postcodes content.njk

{{ govukDetails({
  summaryText: "Help with postcodes",
  html: "Only full postcodes are allowed, no partials e.g SA1 1AA, not SA1 (do we want both?)
}}

{% if errorMsg != '' %}
  {{ govukErrorSummary({
    titleText: "There is a problem",
    errorList: [
      {
        text: errorMsg
      }
    ]
  }) }}
{% endif %}


